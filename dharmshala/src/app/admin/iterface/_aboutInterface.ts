export interface adminCreateAccount{
    id:number,
    trustImage:string | null,
    trustEmailId:string | null,
    trustPhoneNumber:string | null,
    trustAddress:string | null,
    bookingNumber:string | null
}